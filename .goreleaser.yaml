version: 1

project_name: gitdir

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./main.go

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip

nfpms:
  - id: default
    package_name: gitdir
    vendor: Ameen-Sha-Cheerangan
    homepage: https://github.com/Ameen-Sha-Cheerangan/GitDir
    maintainer: Ameen-Sha-Cheerangan
    description: Download specific directories from GitHub repositories at lightning speed.
    license: MIT
    formats:
      - deb
      - rpm
      - apk

brews:
  - repository:
      owner: Ameen-Sha-Cheerangan
      name: homebrew-tap
    commit_author:
      name: Ameen-Sha-Cheerangan
      email: ameenshac981@gmail.com
    commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"
    description: "Download specific directories from GitHub repositories at lightning speed."
    homepage: "https://github.com/Ameen-Sha-Cheerangan/GitDir"
    license: "MIT"
    
scoops:
  - repository:
      owner: Ameen-Sha-Cheerangan
      name: scoop-bucket
    commit_author:
      name: Ameen-Sha-Cheerangan
      email: ameenshac981@gmail.com
    commit_msg_template: "Scoop update for {{ .ProjectName }} version {{ .Tag }}"
    description: "Download specific directories from GitHub repositories at lightning speed."
    homepage: "https://github.com/Ameen-Sha-Cheerangan/GitDir"
    license: "MIT"

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
